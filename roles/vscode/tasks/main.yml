---
- name: Configura o repositorio do VScode
  ansible.builtin.copy:
    src: vscode.repo
    dest: /etc/yum.repos.d/vscode.repo
    mode: '644'
    owner: root
    group: root

- name: Instala o VScode
  ansible.builtin.dnf:
    name: code
    state: present
    update_cache: true
