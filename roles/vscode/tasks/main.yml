---
- name: Configure VSCode repository
  ansible.builtin.copy:
    src: vscode.repo
    dest: /etc/yum.repos.d/vscode.repo
    mode: '644'
    owner: root
    group: root

- name: Install VSCode
  ansible.builtin.dnf:
    name: code
    state: present
    update_cache: true
