---
- name: Enable SSH service
  become: true
  ansible.builtin.service:
    name: sshd
    state: started
    enabled: true
  when: services_enable_ssh

- name: Enable Tailscale service
  become: true
  ansible.builtin.service:
    name: tailscaled
    state: started
    enabled: true
