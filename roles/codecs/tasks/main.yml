---
- name: Install multimedia package group
  ansible.builtin.dnf:
    name: '@Multimedia'
    state: present
    install_weak_deps: false
    exclude:
      - 'PackageKit-gstreamer-plugin'

- name: Install FFmpeg codecs
  ansible.builtin.dnf:
    name: ffmpeg
    state: present
    allowerasing: true
